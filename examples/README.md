# Legacy Python Reference Examples

> [!NOTE]
> This folder contains legacy reference source code preserved for architectural and algorithm reference.
> 
> The application itself has been **completely decoupled and rewritten** into:
> - **Modern UI**: React 19 + TypeScript + Vite + Tailwind CSS + Tauri (`src/`)
> - **Modern Backend**: FastAPI REST Sidecar + Python 3.12 (`core/`)
> - **Modern Chess Protocol**: Standard UCI Engine protocol (`core/features/engine/`) and Universal SQLite database schema (`core/persistence/database.py`).
